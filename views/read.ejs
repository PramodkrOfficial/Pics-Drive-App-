<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <source src="" type="">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-full bg-zinc-900 p-10 text-white">
        <a class="text-blue-500" href="/">Back to Home</a>
        <h1 class=" text-3xl tracking-tighter mt-5 mb-5">All Users</h1>
        <div class="users flex gap-3 flex-wrap">
            <% if(users.length> 0) { %>
                <% users.forEach(function(user){ %>
                    <div class="user w-72 min-h-72 p-2 bg-zinc-800 rounded-lg">
                        <div class="w-full h-22 bg-zinc-700 overflow-hidden"></div>
                        <img class="w-full h-52 object-cover object-top" src="<%= user.image %>">
                        <h3 class="text-xl mt-3 tracking-tighter">
                            <%= user.name %>
                        </h3>
                        <h5 class="text-zinc-300"><%= user.email %></h5>
                        <div class="flex gap-3 mt-6">
                            <a class="text-zinc-200" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-400" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <h3 class="text-zinc-500">No users yet</h3>
            <% } %>
                    
        </div>

    </div>

</body>

</html>